import React from 'react';

const Settings: React.FC = () => {
  return (
    <div>
      <h1>System Settings</h1>
      <p>System admin can manage system tags, WhatsApp groups, and internal tokens via backend configuration.</p>
    </div>
  );
};

export default Settings;
